<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Staff</title>
    <style>
      input {
        margin: 5px 0px;
      }
      select {
        margin: 5px 0px;
      }
      .header {
        display: flex;
        justify-content: center;
      }
      .nav a {
        margin: 20px;
      }
      a {
        text-decoration: none;
        color: blue;
        font-size: 18px;
      }
    </style>
  </head>
  <body>

    <h1 align="center">Add Result</h1>
<% result.forEach(function(data){%>

    <form method="post">
      <table align="center">
        <tr>
          <!-- <td>Roll No:</td>
          <td>
            <select name="rno">
              <option selected disabled>Select Roll No</option>
              <% result.forEach(function (result){%>
              <option value="<%= result.rno%>"><%= result.rno%></option>
              <%})%>
            </select>
          </td> -->
          <td>Roll No:</td>
          <td><input type="text" name="rno" value="<%= data.rno%>" readonly></td>
        </tr>
        <tr>
          <!-- <td>Student Name:</td>
          <td>
            <select name="student">
              <option selected disabled>Select Student</option>
              <% result.forEach(function (result){%>
              <option value="<%= result.name%>"><%= result.name%></option>
              <%})%>
            </select>
          </td> -->
          <td>Student Name:</td>
          <td><input type="text" name="student" value="<%= data.name%>" readonly></td>
        </tr>
        <tr>
          <td>Standard:</td>
          <!-- <td>
            <select name="std">
              <option selected disabled>Select STD</option>

              <option value="6">6</option>
              <option value="7">7</option>
              <option value="8">8</option>
            </select>
          </td> -->
          <td><input type="text" name="std" value="<%= data.standard%>" readonly></td>
        </tr>
        <tr>
          <td>Division:</td>
          <!-- <td>
            <select name="division">
              <option selected disabled>Select Division</option>
              <option value="A">A</option>
              <option value="B">B</option>
              <option value="C">C</option>
              <option value="D">D</option>
            </select>
          </td> -->
          <td><input type="text" name="division" value="<%= data.division%>" readonly></td>
        </tr>

        <tr>
          <td>Gujarati:</td>
          <td><input type="text" name="guj" /></td>
        </tr>
        <tr>
          <td>English:</td>
          <td><input type="text" name="eng" /></td>
        </tr>
        <tr>
          <td>Maths:</td>
          <td><input type="text" name="maths" /></td>
        </tr>
        <tr>
          <td>Hindi:</td>
          <td><input type="text" name="hindi" /></td>
        </tr>
        <tr>
          <td>Science:</td>
          <td><input type="text" name="sci" /></td>
        </tr>

        <tr>
          <td></td>
          <td><input type="submit" value="Add Result" /></td>
        </tr>
      </table>
    </form>
<%})%>

  </body>
</html>
